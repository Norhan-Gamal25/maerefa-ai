fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.1

# crewai 1.x — tools package must match crewai version exactly
crewai==1.15.2
crewai-tools==1.15.2

# langchain — crewai 1.x bundles its own langchain deps; keep these loose
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0

# Relax pydantic/httpx/requests — let pip resolve compatible versions
pydantic>=2.7.4,<3.0.0
httpx>=0.27.0,<1.0.0
requests>=2.32.3,<3.0.0

slowapi==0.1.9
selenium==4.18.1
cohere>=5.5.0,<6.0.0
packaging>=24.0
litellm>=1.0.0
