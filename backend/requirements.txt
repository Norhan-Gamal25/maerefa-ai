fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.1

# crewai 1.x — tools package must match crewai version exactly
crewai==1.15.2
crewai-tools==1.15.2

# crewai 1.15.2 requires pydantic>=2.11.9,<2.13 — pin strictly
pydantic>=2.11.9,<2.13.0
pydantic-settings~=2.10.1

# langchain — crewai 1.x bundles its own langchain deps; keep these loose
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0

# httpx/requests
httpx>=0.27.0,<1.0.0
requests>=2.32.3,<3.0.0

# opentelemetry — pin to a single coherent version set
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1
opentelemetry-proto==1.42.1
opentelemetry-exporter-otlp-proto-common==1.42.1
opentelemetry-exporter-otlp-proto-grpc==1.42.1

# protobuf must stay <7.0 for opentelemetry-proto 1.42.1
protobuf>=5.0,<7.0

# websockets must stay <16 for langgraph-sdk 0.4.x
websockets>=14.0,<16.0

# rich must stay <15 for instructor
rich>=13.7.0,<15.0.0

slowapi==0.1.9
selenium==4.18.1
cohere>=5.5.0,<6.0.0
packaging>=24.0
litellm>=1.0.0
